meta {
  name: search-ad-limit
  type: http
  seq: 3
}

get {
  url: {{localhost}}/ad
  body: none
  auth: none
}

tests {
  test("Insert ad, country, platform", function() {
    expect(res.status).to.equal(200);
  });
}
