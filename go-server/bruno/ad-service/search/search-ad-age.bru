meta {
  name: search-ad-age
  type: http
  seq: 4
}

get {
  url: {{localhost}}/ad?offset=0&limit=10&country=TW&platform=ios&gender=F
  body: none
  auth: none
}

query {
  offset: 0
  limit: 10
  country: TW
  platform: ios
  gender: F
}

tests {
  test("Insert ad, country, platform", function() {
    expect(res.status).to.equal(200);
  });
}
